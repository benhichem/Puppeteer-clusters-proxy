{
	"name": "multi-proxy-clusters",
	"version": "1.0.0",
	"description": "multi clusters puppeteer with rotation proxies",
	"main": "index.js",
	"scripts": {
		"start": "node main",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "@BenHichem & @Aesth",
	"license": "ISC",
	"dependencies": {
		"puppeteer": "^15.4.0"
	}
}
